[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "disttrainenv"
version = "1.0.0"
dependencies = [
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "app:main"